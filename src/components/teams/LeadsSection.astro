---
import LeadCard from "./LeadCard.astro";
import leadsData from "../../data/leads.json";
---

<section class="leads-section">
    <div class="container">
        <div class="section-header">
            <span class="label" data-aos="fade-up" data-aos-duration="600">CORE LEADERSHIP</span>
            <h2 class="section-title" data-aos="fade-up" data-aos-delay="150" data-aos-duration="800">THE LEADS</h2>
        </div>
        
        <div class="leads-grid">
            {leadsData.map((lead, index) => (
                <LeadCard 
                    name={lead.name}
                    role={lead.role}
                    image={lead.image}
                    index={index}
                />
            ))}
        </div>
    </div>
</section>

<style>
    .leads-section {
        padding: 4rem 2rem 10rem;
        background-color: #000;
        position: relative;
        z-index: 10;
    }

    .container {
        max-width: 1400px;
        margin: 0 auto;
    }

    .section-header {
        margin-bottom: 5rem;
        text-align: left;
    }

    .label {
        color: #00f5d4;
        font-size: 0.85rem;
        letter-spacing: 0.3rem;
        font-weight: 700;
        text-transform: uppercase;
        margin-bottom: 1rem;
        display: block;
    }

    .section-title {
        font-size: clamp(2.5rem, 6vw, 4.5rem);
        font-weight: 900;
        color: white;
        margin: 0;
        text-transform: uppercase;
        letter-spacing: -0.02em;
        line-height: 1;
    }

    .leads-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(210px, max-content));
        gap: 1.5rem;

        justify-content: center;
    }

    @media (max-width: 1280px) {
        .leads-grid {
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
        }
    }

    @media (max-width: 768px) {
        .leads-grid {
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
        }
    }

    @media (max-width: 480px) {
        .leads-grid {
            grid-template-columns: 1fr;
            gap: 2rem;
        }
    }
</style>
